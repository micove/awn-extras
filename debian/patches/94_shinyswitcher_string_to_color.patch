diff -Nur -x '*.orig' -x '*~' awn-extras-applets-0.2.6/src/shiny-switcher/shinyswitcherapplet.c awn-extras-applets-0.2.6.new/src/shiny-switcher/shinyswitcherapplet.c
--- awn-extras-applets-0.2.6/src/shiny-switcher/shinyswitcherapplet.c	2008-02-14 17:49:53.000000000 +0100
+++ awn-extras-applets-0.2.6.new/src/shiny-switcher/shinyswitcherapplet.c	2008-10-14 21:49:24.000000000 +0200
@@ -83,8 +83,16 @@
 static void config_get_color (AwnConfigClient *client, const gchar *key, AwnColor *color)
 {
 	gchar *value = awn_config_client_get_string (client, AWN_CONFIG_CLIENT_DEFAULT_GROUP, key, NULL);
-	awn_cairo_string_to_color (value, color);
-	g_free (value);
+	if (value) 
+	{
+		awn_cairo_string_to_color (value, color); 
+		g_free (value);
+	}
+	else
+	{
+		g_warning("Failed to read config key: %s\n",key); 
+		awn_cairo_string_to_color ("000000", color); 
+	}
 }
 
 void init_config(Shiny_switcher *shinyswitcher)
